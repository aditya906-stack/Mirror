# Mirror — environment configuration
# Copy this file to .env and adjust as needed.
#
# SQLite database. The path is resolved relative to the project root.
# After setting this, run:  bun run db:push   (creates the database file)

DATABASE_URL=file:./db/custom.db
